export enum Status {
  Inactive,
  Active,
}

export type StatusName = 'Active' | 'Inactive';

export enum AppointmentStatus {
  Pending,
  Approved,
  Cancelled,
}

export enum ReadStatus {
  Unread,
  Read,
}
